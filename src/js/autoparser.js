let parserInput = ""

function ParseInput() {
   # Check the clangd version
   GetSpecifiedClangVersion()

   # Fetch the input

   # Attempt to parse all version-compatible rules
   # Check which default style is the most similar, then use that in the 
   # BasedOnStyle rule.
   Rule_BasedOnStyle()
}

function GetSpecifiedClangVersion() {

}

function GetUserInput() {

}

function Rule_ () 

function Rule_BasedOnStyle() {
   # Find the style with the fewest rule deviations
   
}
